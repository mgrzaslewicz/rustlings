# Notes

- User is an experienced Java/Kotlin developer — lean on contrasting Rust references against JVM object-reference semantics rather than explaining from a blank slate.
- Working inside the `rustlings` repo itself. Tie every lesson back to a concrete exercise file under `exercises/` where one exists.
- User asked for lessons under `learning/` specifically, not the repo root — keep using that path.
